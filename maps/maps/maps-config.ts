export type MapsConfig = {
  /**
   * Path under which the map page is registered.
   */
  mapPath?: string;

  /**
   * Label rendered next to the map navigation item.
   */
  navigationLabel?: string;
};

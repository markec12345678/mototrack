import { darkThemeSchema } from './dark-tokens.js';

/**
 * Additional MotoTrack theme variations.
 * The default theme is defined in mototrack-tokens.ts and is not listed here.
 */
export const themeOptions = {
  dark: darkThemeSchema,
};

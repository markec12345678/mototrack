---
description: 'A mototrack-prototype app.'
labels: ['app', 'react']
---

A React application for mototrack-prototype. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use mototrack-prototype 
```

To run the app locally, run the following:

```
bit run mototrack-prototype 
```
